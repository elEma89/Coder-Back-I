import ProductManager from "./ProductManager.js";

const ProductManager = new ProductManager("./products.json");

const main = async() => {
    try {

    } catch (error) {
        console.log(error);
    }
}